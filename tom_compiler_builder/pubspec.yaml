name: tom_compiler_builder
description: Build_runner builder and CLI tool for cross-platform Dart compilation.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  build: ^4.0.4
  glob: ^2.1.3
  path: ^1.9.0
  yaml: ^3.1.2
  args: ^2.7.0
  tom_build_base:
    path: ../tom_build_base

dev_dependencies:
  build_runner: ^2.4.15
  lints: ^6.0.0
  test: ^1.25.6
  tom_version_builder:
    path: ../tom_version_builder

executables:
  compiler:
