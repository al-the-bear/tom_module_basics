name: test_cleanup_project
description: Test project for cleanup builder
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  tom_build_base:
    path: ../../tom_build_base
  yaml: ^3.1.3
