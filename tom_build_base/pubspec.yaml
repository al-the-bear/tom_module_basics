name: tom_build_base
description: Shared utilities for Tom build tools — configuration loading from buildkit.yaml, project scanning, path validation, and build.yaml utilities.
version: 2.1.0
repository: https://github.com/al-the-bear/tom_module_basics
homepage: https://github.com/al-the-bear/tom_module_basics/tree/main/tom_build_base
topics:
  - build-tools
  - cli
  - configuration

executables:
  show_versions:

environment:
  sdk: ^3.10.4

dependencies:
  args: ^2.7.0
  dcli: ^8.4.2
  glob: ^2.1.3
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
