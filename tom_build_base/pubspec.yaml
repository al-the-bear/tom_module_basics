name: tom_build_base
description: Shared utilities for Tom build tools - config loading, path validation, and project scanning.
version: 0.1.0
repository: https://github.com/nickmeinhold/tom

environment:
  sdk: ^3.10.4

dependencies:
  glob: ^2.1.3
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
